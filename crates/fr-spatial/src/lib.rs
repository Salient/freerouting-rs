//! fr-spatial: R-tree spatial index over fr-geometry shapes (obstacle index used by
//! lazy expansion-room completion). Phase 2 wraps `rstar`.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() { assert_eq!(2 + 2, 4); }
}
