//! freerouting-rs CLI: headless route/convert entry point used by the self-verifying
//! gates and the Altium round-trip harness.
//! Usage (target): freerouting-rs route IN.dsn -o OUT.rte [--max-time S] [--threads N]

fn main() {
    println!("freerouting-rs (skeleton) - see freerouting-rs-spec/MILESTONES.md");
}
