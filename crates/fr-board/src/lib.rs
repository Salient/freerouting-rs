//! fr-board: the board data model - layers, padstacks (incl. shapeless/no-copper),
//! components, pins, traces, vias, nets, rules, clearance matrix, board outline.
//! Phase 3.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() { assert_eq!(2 + 2, 4); }
}
